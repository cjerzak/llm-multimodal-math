# Required for experiment runs that call the Tinker API.
TINKER_API_KEY=

# Optional: needed when tokenizer/model access requires Hugging Face auth.
HF_TOKEN=

# Optional: uncomment to force offline-only Hugging Face behavior.
# HF_HUB_OFFLINE=1
